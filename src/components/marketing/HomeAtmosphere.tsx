"use client";

/**
 * Solid dark canvas for the marketing home — no photo, no glow stack.
 */
export function HomeAtmosphere() {
  return (
    <div
      aria-hidden
      className="pointer-events-none fixed inset-0 -z-10 bg-[#0c0c0d]"
    />
  );
}
