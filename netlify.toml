[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "20"

# Next.js Runtime is applied automatically by Netlify for App Router sites.
# Do not set a static "publish" directory — the adapter handles it.
